Let's go. Step by step, in order, no rushing.

UC15 Complete Walkthrough

STEP 1 — Update IMeasurable.java
Open your existing IMeasurable.java and add exactly these 2 lines to the interface:
javaString getMeasurementType();
IMeasurable getUnitByName(String unitName);
Your complete file should look like this:
javapackage com.bridgelabz;

public interface IMeasurable {

    double convertToBaseUnit(double value);
    double convertFromBaseUnit(double baseValue);
    
    default void validateOperationSupport(String operationName) {
        // default: all operations supported
    }

    // UC15 additions
    String getMeasurementType();
    IMeasurable getUnitByName(String unitName);
}

STEP 2 — Update all 4 Enums
Open each enum and add these 2 methods at the bottom, before the closing }.
LengthUnit.java — add:
java@Override
public String getMeasurementType() { 
    return "LENGTH"; 
}

@Override
public IMeasurable getUnitByName(String unitName) {
    return LengthUnit.valueOf(unitName.toUpperCase());
}
WeightUnit.java — add:
java@Override
public String getMeasurementType() { 
    return "WEIGHT"; 
}

@Override
public IMeasurable getUnitByName(String unitName) {
    return WeightUnit.valueOf(unitName.toUpperCase());
}
VolumeUnit.java — add:
java@Override
public String getMeasurementType() { 
    return "VOLUME"; 
}

@Override
public IMeasurable getUnitByName(String unitName) {
    return VolumeUnit.valueOf(unitName.toUpperCase());
}
TemperatureUnit.java — add:
java@Override
public String getMeasurementType() { 
    return "TEMPERATURE"; 
}

@Override
public IMeasurable getUnitByName(String unitName) {
    return TemperatureUnit.valueOf(unitName.toUpperCase());
}
✅ Check: Run your existing tests now. All UC1–UC14 tests should still pass.

STEP 3 — Update Quantity.java
Check if these methods exist. If not, add them:
java// Add getter for value
public double getValue() { 
    return value; 
}

// Add getter for unit
public U getUnit() { 
    return unit; 
}

// Add subtract if missing
public Quantity<U> subtract(Quantity<U> other, U targetUnit) {
    double base = performBaseArithmetic(other, ArithmeticOperation.SUBTRACT);
    return new Quantity<>(roundToTwoDecimals(
        targetUnit.convertFromBaseUnit(base)), targetUnit);
}

// Add divide if missing
public double divide(Quantity<U> other) {
    double base1 = this.unit.convertToBaseUnit(this.value);
    double base2 = other.unit.convertToBaseUnit(other.value);
    if (base2 == 0.0) throw new ArithmeticException("Division by zero");
    return roundToTwoDecimals(base1 / base2);
}
✅ Check: Run tests again. Still all passing.

STEP 4 — Create QuantityMeasurementException.java
Create a new file in com.bridgelabz:
javapackage com.bridgelabz;

public class QuantityMeasurementException extends RuntimeException {

    public QuantityMeasurementException(String message) {
        super(message);
    }

    public QuantityMeasurementException(String message, Throwable cause) {
        super(message, cause);
    }
}
What this does: Custom exception so all errors in UC15 are caught and reported cleanly instead of crashing the app.

STEP 5 — Create QuantityDTO.java
Create a new file in com.bridgelabz:
javapackage com.bridgelabz;

public class QuantityDTO {

    private double value;
    private String unit;
    private String measurementType;

    public QuantityDTO(double value, String unit, String measurementType) {
        this.value = value;
        this.unit = unit;
        this.measurementType = measurementType;
    }

    public double getValue() { return value; }
    public String getUnit() { return unit; }
    public String getMeasurementType() { return measurementType; }

    @Override
    public String toString() {
        return value + " " + unit;
    }
}
What this does: This is the object that travels between layers. The controller creates it, passes it to the service, service returns it. Uses Strings for unit and type so it's not coupled to any enum.

STEP 6 — Create QuantityModel.java
Create a new file in com.bridgelabz:
javapackage com.bridgelabz;

public class QuantityModel<U extends IMeasurable> {

    private double value;
    private U unit;

    public QuantityModel(double value, U unit) {
        this.value = value;
        this.unit = unit;
    }

    public double getValue() { return value; }
    public U getUnit() { return unit; }

    @Override
    public String toString() {
        return value + " " + unit;
    }
}
What this does: This is the internal representation inside the service layer. Unlike QuantityDTO which uses Strings, this holds the actual typed enum unit so Quantity<U> can work with it.

STEP 7 — Create QuantityMeasurementEntity.java
Create a new file in com.bridgelabz:
javapackage com.bridgelabz;

import java.io.Serializable;
import java.time.LocalDateTime;

public class QuantityMeasurementEntity implements Serializable {

    private static final long serialVersionUID = 1L;

    private String operationType;
    private String operand1;
    private String operand2;
    private String result;
    private boolean hasError;
    private String errorMessage;
    private LocalDateTime timestamp;

    // For single-operand operations like CONVERT
    public QuantityMeasurementEntity(String operationType, 
                                      String operand1, 
                                      String result) {
        this.operationType = operationType;
        this.operand1 = operand1;
        this.operand2 = null;
        this.result = result;
        this.hasError = false;
        this.errorMessage = null;
        this.timestamp = LocalDateTime.now();
    }

    // For binary operations like ADD, COMPARE
    public QuantityMeasurementEntity(String operationType, 
                                      String operand1, 
                                      String operand2, 
                                      String result) {
        this.operationType = operationType;
        this.operand1 = operand1;
        this.operand2 = operand2;
        this.result = result;
        this.hasError = false;
        this.errorMessage = null;
        this.timestamp = LocalDateTime.now();
    }

    // For errors
    public QuantityMeasurementEntity(String operationType, 
                                      String operand1, 
                                      String operand2, 
                                      String errorMessage, 
                                      boolean hasError) {
        this.operationType = operationType;
        this.operand1 = operand1;
        this.operand2 = operand2;
        this.result = null;
        this.hasError = hasError;
        this.errorMessage = errorMessage;
        this.timestamp = LocalDateTime.now();
    }

    public String getOperationType() { return operationType; }
    public String getOperand1() { return operand1; }
    public String getOperand2() { return operand2; }
    public String getResult() { return result; }
    public boolean hasError() { return hasError; }
    public String getErrorMessage() { return errorMessage; }
    public LocalDateTime getTimestamp() { return timestamp; }

    @Override
    public String toString() {
        if (hasError)
            return "[" + timestamp + "] " + operationType 
                   + "(" + operand1 
                   + (operand2 != null ? ", " + operand2 : "") 
                   + ") => ERROR: " + errorMessage;

        return "[" + timestamp + "] " + operationType 
               + "(" + operand1 
               + (operand2 != null ? ", " + operand2 : "") 
               + ") => " + result;
    }
}
What this does: Every operation — success or failure — gets saved as one of these. The repository stores a list of them. Think of it as the audit log.

STEP 8 — Create IQuantityMeasurementRepository.java
Create a new file in com.bridgelabz:
javapackage com.bridgelabz;

import java.util.List;

public interface IQuantityMeasurementRepository {

    void save(QuantityMeasurementEntity entity);

    List<QuantityMeasurementEntity> getAllMeasurements();
}
What this does: The contract. The service only ever talks to this interface, never directly to the cache class. This means you could swap in a database later without touching the service.

STEP 9 — Create QuantityMeasurementCacheRepository.java
Create a new file in com.bridgelabz:
javapackage com.bridgelabz;

import java.io.*;
import java.util.ArrayList;
import java.util.List;

public class QuantityMeasurementCacheRepository 
    implements IQuantityMeasurementRepository {

    private static final String FILE_PATH = "quantity_measurements.ser";
    private static QuantityMeasurementCacheRepository instance;
    private List<QuantityMeasurementEntity> cache;

    private QuantityMeasurementCacheRepository() {
        this.cache = new ArrayList<>();
        loadFromDisk();
    }

    public static synchronized QuantityMeasurementCacheRepository getInstance() {
        if (instance == null) {
            instance = new QuantityMeasurementCacheRepository();
        }
        return instance;
    }

    @Override
    public void save(QuantityMeasurementEntity entity) {
        cache.add(entity);
        saveToDisk(entity);
    }

    @Override
    public List<QuantityMeasurementEntity> getAllMeasurements() {
        return new ArrayList<>(cache);
    }

    private void saveToDisk(QuantityMeasurementEntity entity) {
        File file = new File(FILE_PATH);
        try {
            if (file.exists()) {
                try (AppendableObjectOutputStream oos =
                    new AppendableObjectOutputStream(
                        new FileOutputStream(file, true))) {
                    oos.writeObject(entity);
                }
            } else {
                try (ObjectOutputStream oos =
                    new ObjectOutputStream(
                        new FileOutputStream(file))) {
                    oos.writeObject(entity);
                }
            }
        } catch (IOException e) {
            System.err.println("Warning: Could not save to disk - " 
                + e.getMessage());
        }
    }

    private void loadFromDisk() {
        File file = new File(FILE_PATH);
        if (!file.exists()) return;
        try (ObjectInputStream ois = 
            new ObjectInputStream(new FileInputStream(file))) {
            while (true) {
                try {
                    QuantityMeasurementEntity entity =
                        (QuantityMeasurementEntity) ois.readObject();
                    cache.add(entity);
                } catch (EOFException e) {
                    break;
                }
            }
        } catch (IOException | ClassNotFoundException e) {
            System.err.println("Warning: Could not load from disk - " 
                + e.getMessage());
        }
    }

    private static class AppendableObjectOutputStream 
        extends ObjectOutputStream {

        public AppendableObjectOutputStream(OutputStream out) 
            throws IOException {
            super(out);
        }

        @Override
        protected void writeStreamHeader() throws IOException {
            reset();
        }
    }
}
What this does: Singleton — only one instance ever exists. Keeps an in-memory list AND writes to a .ser file on disk so history survives app restarts. The inner AppendableObjectOutputStream class is needed so appending to the file doesn't corrupt the stream header.

STEP 10 — Create IQuantityMeasurementService.java
Create a new file in com.bridgelabz:
javapackage com.bridgelabz;

public interface IQuantityMeasurementService {

    QuantityDTO compare(QuantityDTO q1, QuantityDTO q2);

    QuantityDTO convert(QuantityDTO source, QuantityDTO target);

    QuantityDTO add(QuantityDTO q1, QuantityDTO q2, QuantityDTO targetUnit);

    QuantityDTO subtract(QuantityDTO q1, QuantityDTO q2, QuantityDTO targetUnit);

    QuantityDTO divide(QuantityDTO q1, QuantityDTO q2);
}
What this does: The controller only ever calls these methods. It never knows about QuantityMeasurementServiceImpl directly.

STEP 11 — Create QuantityMeasurementServiceImpl.java
Create a new file in com.bridgelabz:
javapackage com.bridgelabz;

public class QuantityMeasurementServiceImpl 
    implements IQuantityMeasurementService {

    private final IQuantityMeasurementRepository repository;

    public QuantityMeasurementServiceImpl(
            IQuantityMeasurementRepository repository) {
        if (repository == null)
            throw new QuantityMeasurementException(
                "Repository cannot be null");
        this.repository = repository;
    }

    // Converts QuantityDTO → QuantityModel (String unit → actual enum)
    @SuppressWarnings("unchecked")
    private <U extends IMeasurable> QuantityModel<U> toModel(QuantityDTO dto) {
        IMeasurable reference = getReferenceUnit(dto.getMeasurementType());
        U unit = (U) reference.getUnitByName(dto.getUnit());
        return new QuantityModel<>(dto.getValue(), unit);
    }

    // Returns one enum constant to use as a factory for getUnitByName()
    private IMeasurable getReferenceUnit(String type) {
        return switch (type.toUpperCase()) {
            case "LENGTH"      -> LengthUnit.INCH;
            case "WEIGHT"      -> WeightUnit.GRAM;
            case "VOLUME"      -> VolumeUnit.MILLILITER;
            case "TEMPERATURE" -> TemperatureUnit.CELSIUS;
            default -> throw new QuantityMeasurementException(
                "Unknown measurement type: " + type);
        };
    }

    private void validateSameCategory(QuantityDTO q1, QuantityDTO q2, 
                                       String operation) {
        if (!q1.getMeasurementType()
                .equalsIgnoreCase(q2.getMeasurementType())) {
            throw new QuantityMeasurementException(
                "Cannot " + operation + " different measurement types: "
                + q1.getMeasurementType() + " vs " 
                + q2.getMeasurementType());
        }
    }

    @Override
    public QuantityDTO compare(QuantityDTO q1, QuantityDTO q2) {
        try {
            if (q1 == null || q2 == null)
                throw new QuantityMeasurementException(
                    "Input cannot be null");
            validateSameCategory(q1, q2, "compare");

            QuantityModel<IMeasurable> m1 = toModel(q1);
            QuantityModel<IMeasurable> m2 = toModel(q2);

            Quantity<IMeasurable> qty1 = 
                new Quantity<>(m1.getValue(), m1.getUnit());
            Quantity<IMeasurable> qty2 = 
                new Quantity<>(m2.getValue(), m2.getUnit());

            boolean isEqual = qty1.equals(qty2);

            repository.save(new QuantityMeasurementEntity(
                "COMPARE", q1.toString(), q2.toString(), 
                String.valueOf(isEqual)));

            return new QuantityDTO(isEqual ? 1.0 : 0.0, "BOOLEAN", "RESULT");

        } catch (QuantityMeasurementException e) {
            repository.save(new QuantityMeasurementEntity(
                "COMPARE",
                q1 != null ? q1.toString() : "null",
                q2 != null ? q2.toString() : "null",
                e.getMessage(), true));
            throw e;
        }
    }

    @Override
    public QuantityDTO convert(QuantityDTO source, QuantityDTO target) {
        try {
            if (source == null || target == null)
                throw new QuantityMeasurementException(
                    "Input cannot be null");
            validateSameCategory(source, target, "convert");

            QuantityModel<IMeasurable> srcModel = toModel(source);
            QuantityModel<IMeasurable> tgtModel = toModel(target);

            Quantity<IMeasurable> qty = 
                new Quantity<>(srcModel.getValue(), srcModel.getUnit());
            Quantity<IMeasurable> converted = 
                qty.convertTo(tgtModel.getUnit());

            QuantityDTO result = new QuantityDTO(
                converted.getValue(), 
                target.getUnit(), 
                target.getMeasurementType());

            repository.save(new QuantityMeasurementEntity(
                "CONVERT", source.toString(), result.toString()));

            return result;

        } catch (QuantityMeasurementException e) {
            repository.save(new QuantityMeasurementEntity(
                "CONVERT",
                source != null ? source.toString() : "null",
                null, e.getMessage(), true));
            throw e;
        }
    }

    @Override
    public QuantityDTO add(QuantityDTO q1, QuantityDTO q2, 
                            QuantityDTO targetUnit) {
        try {
            if (q1 == null || q2 == null || targetUnit == null)
                throw new QuantityMeasurementException(
                    "Input cannot be null");
            validateSameCategory(q1, q2, "add");

            QuantityModel<IMeasurable> m1 = toModel(q1);
            QuantityModel<IMeasurable> m2 = toModel(q2);
            QuantityModel<IMeasurable> target = toModel(targetUnit);

            Quantity<IMeasurable> qty1 = 
                new Quantity<>(m1.getValue(), m1.getUnit());
            Quantity<IMeasurable> qty2 = 
                new Quantity<>(m2.getValue(), m2.getUnit());

            Quantity<IMeasurable> sum = qty1.add(qty2, target.getUnit());

            QuantityDTO result = new QuantityDTO(
                sum.getValue(), 
                targetUnit.getUnit(), 
                targetUnit.getMeasurementType());

            repository.save(new QuantityMeasurementEntity(
                "ADD", q1.toString(), q2.toString(), result.toString()));

            return result;

        } catch (UnsupportedOperationException e) {
            repository.save(new QuantityMeasurementEntity(
                "ADD", q1.toString(), q2.toString(), 
                e.getMessage(), true));
            throw new QuantityMeasurementException(e.getMessage(), e);
        } catch (QuantityMeasurementException e) {
            throw e;
        }
    }

    @Override
    public QuantityDTO subtract(QuantityDTO q1, QuantityDTO q2, 
                                 QuantityDTO targetUnit) {
        try {
            if (q1 == null || q2 == null || targetUnit == null)
                throw new QuantityMeasurementException(
                    "Input cannot be null");
            validateSameCategory(q1, q2, "subtract");

            QuantityModel<IMeasurable> m1 = toModel(q1);
            QuantityModel<IMeasurable> m2 = toModel(q2);
            QuantityModel<IMeasurable> target = toModel(targetUnit);

            Quantity<IMeasurable> qty1 = 
                new Quantity<>(m1.getValue(), m1.getUnit());
            Quantity<IMeasurable> qty2 = 
                new Quantity<>(m2.getValue(), m2.getUnit());

            Quantity<IMeasurable> diff = 
                qty1.subtract(qty2, target.getUnit());

            QuantityDTO result = new QuantityDTO(
                diff.getValue(), 
                targetUnit.getUnit(), 
                targetUnit.getMeasurementType());

            repository.save(new QuantityMeasurementEntity(
                "SUBTRACT", q1.toString(), q2.toString(), 
                result.toString()));

            return result;

        } catch (UnsupportedOperationException e) {
            repository.save(new QuantityMeasurementEntity(
                "SUBTRACT", q1.toString(), q2.toString(), 
                e.getMessage(), true));
            throw new QuantityMeasurementException(e.getMessage(), e);
        } catch (QuantityMeasurementException e) {
            throw e;
        }
    }

    @Override
    public QuantityDTO divide(QuantityDTO q1, QuantityDTO q2) {
        try {
            if (q1 == null || q2 == null)
                throw new QuantityMeasurementException(
                    "Input cannot be null");
            validateSameCategory(q1, q2, "divide");

            QuantityModel<IMeasurable> m1 = toModel(q1);
            QuantityModel<IMeasurable> m2 = toModel(q2);

            Quantity<IMeasurable> qty1 = 
                new Quantity<>(m1.getValue(), m1.getUnit());
            Quantity<IMeasurable> qty2 = 
                new Quantity<>(m2.getValue(), m2.getUnit());

            double ratio = qty1.divide(qty2);

            repository.save(new QuantityMeasurementEntity(
                "DIVIDE", q1.toString(), q2.toString(), 
                String.valueOf(ratio)));

            return new QuantityDTO(ratio, "SCALAR", "RESULT");

        } catch (ArithmeticException e) {
            repository.save(new QuantityMeasurementEntity(
                "DIVIDE", q1.toString(), q2.toString(), 
                e.getMessage(), true));
            throw new QuantityMeasurementException(
                "Division by zero", e);
        } catch (QuantityMeasurementException e) {
            throw e;
        }
    }
}

STEP 12 — Create QuantityMeasurementController.java
Create a new file in com.bridgelabz:
javapackage com.bridgelabz;

public class QuantityMeasurementController {

    private final IQuantityMeasurementService service;

    public QuantityMeasurementController(
            IQuantityMeasurementService service) {
        if (service == null)
            throw new QuantityMeasurementException(
                "Service cannot be null");
        this.service = service;
    }

    public void performComparison(QuantityDTO q1, QuantityDTO q2) {
        System.out.println("\n[COMPARE] " + q1 + " vs " + q2);
        try {
            QuantityDTO result = service.compare(q1, q2);
            System.out.println("Result: " + (result.getValue() == 1.0));
        } catch (QuantityMeasurementException e) {
            System.out.println("Error: " + e.getMessage());
        }
    }

    public void performConversion(QuantityDTO source, 
                                   QuantityDTO targetUnit) {
        System.out.println("\n[CONVERT] " + source 
            + " => " + targetUnit.getUnit());
        try {
            QuantityDTO result = service.convert(source, targetUnit);
            System.out.println("Result: " + result);
        } catch (QuantityMeasurementException e) {
            System.out.println("Error: " + e.getMessage());
        }
    }

    public void performAddition(QuantityDTO q1, QuantityDTO q2, 
                                 QuantityDTO targetUnit) {
        System.out.println("\n[ADD] " + q1 + " + " + q2);
        try {
            QuantityDTO result = service.add(q1, q2, targetUnit);
            System.out.println("Result: " + result);
        } catch (QuantityMeasurementException e) {
            System.out.println("Error: " + e.getMessage());
        }
    }

    public void performSubtraction(QuantityDTO q1, QuantityDTO q2, 
                                    QuantityDTO targetUnit) {
        System.out.println("\n[SUBTRACT] " + q1 + " - " + q2);
        try {
            QuantityDTO result = service.subtract(q1, q2, targetUnit);
            System.out.println("Result: " + result);
        } catch (QuantityMeasurementException e) {
            System.out.println("Error: " + e.getMessage());
        }
    }

    public void performDivision(QuantityDTO q1, QuantityDTO q2) {
        System.out.println("\n[DIVIDE] " + q1 + " / " + q2);
        try {
            QuantityDTO result = service.divide(q1, q2);
            System.out.println("Result: " + result.getValue());
        } catch (QuantityMeasurementException e) {
            System.out.println("Error: " + e.getMessage());
        }
    }
}

STEP 13 — Refactor QuantityMeasurementApp.java
Replace the entire existing file with this:
javapackage com.bridgelabz;

public class QuantityMeasurementApp {

    public static void main(String[] args) {

        // Layer initialization via dependency injection
        IQuantityMeasurementRepository repository =
            QuantityMeasurementCacheRepository.getInstance();

        IQuantityMeasurementService service =
            new QuantityMeasurementServiceImpl(repository);

        QuantityMeasurementController controller =
            new QuantityMeasurementController(service);

        // 1. Length equality
        System.out.println("=== Length Equality ===");
        controller.performComparison(
            new QuantityDTO(1.0, "FEET", "LENGTH"),
            new QuantityDTO(12.0, "INCHES", "LENGTH")
        );

        // 2. Length conversion
        System.out.println("\n=== Length Conversion ===");
        controller.performConversion(
            new QuantityDTO(1.0, "FEET", "LENGTH"),
            new QuantityDTO(0.0, "INCHES", "LENGTH")
        );

        // 3. Length addition
        System.out.println("\n=== Length Addition ===");
        controller.performAddition(
            new QuantityDTO(1.0, "YARDS", "LENGTH"),
            new QuantityDTO(3.0, "FEET", "LENGTH"),
            new QuantityDTO(0.0, "YARDS", "LENGTH")
        );

        // 4. Temperature comparison
        System.out.println("\n=== Temperature Comparison ===");
        controller.performComparison(
            new QuantityDTO(100.0, "CELSIUS", "TEMPERATURE"),
            new QuantityDTO(212.0, "FAHRENHEIT", "TEMPERATURE")
        );

        // 5. Temperature addition — should fail gracefully
        System.out.println("\n=== Temperature Addition (unsupported) ===");
        controller.performAddition(
            new QuantityDTO(100.0, "CELSIUS", "TEMPERATURE"),
            new QuantityDTO(50.0, "CELSIUS", "TEMPERATURE"),
            new QuantityDTO(0.0, "CELSIUS", "TEMPERATURE")
        );

        // 6. Cross-category — should fail gracefully
        System.out.println("\n=== Cross-Category (should error) ===");
        controller.performComparison(
            new QuantityDTO(1.0, "FEET", "LENGTH"),
            new QuantityDTO(1.0, "KILOGRAM", "WEIGHT")
        );
    }
}

Final Checklist
Before running, verify this:
#What to check1IMeasurable has getMeasurementType() and getUnitByName()2All 4 enums implement both new methods3Quantity.java has getValue(), getUnit(), subtract(), divide()4All 9 new files exist in com.bridgelabz5QuantityMeasurementApp no longer has static demo methods6All UC1–UC14 tests still pass
Run the app — you should see clean output for all 6 scenarios, with errors reported gracefully instead of crashing.